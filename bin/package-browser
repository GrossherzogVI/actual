#!/bin/bash -e

# Desktop-client removed — Level-5 web (apps/web) is the primary frontend.
# This script now builds the Level-5 web app.

ROOT=`dirname $0`
cd "$ROOT/.."

export NODE_OPTIONS="--max-old-space-size=4096"

yarn workspace @finance-os/web build

echo "apps/web/dist"
